# Leave empty for local dev when Vite proxies /api to the API (see vite.config.ts).
# For split deploy (Vercel + Render), set to your API origin, e.g.:
# VITE_API_BASE_URL=https://your-api.onrender.com
